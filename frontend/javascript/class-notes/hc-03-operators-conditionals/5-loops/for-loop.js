/* -------------------------------------------------------------------------- */
//!                                 FOR LOOP                                  */
/* -------------------------------------------------------------------------- */
// for (başlangıç; koşul; artış) {
//     // Döngü bloğu
//   }

// Başlangıç: Döngünün başlangıcında yapılacak işlem. Bu genellikle bir sayaç değişkeni tanımlar.
// Koşul: Döngünün çalışmaya devam etmesi için gereken koşuldur. Koşul sağlanmadığında döngü sona erer.
// Artış: Her döngü sonunda sayaç değişkeninin nasıl değişeceğini belirler.
for (let i = 0; i < 5; i++) {
    console.log(i);
}
// Program that takes a person's two grades and finds their average
// let sum = 0;
// for (let t = 1; t <= 2; t++) {
//     const not1 = +prompt("1. enter note")
//     const not2 = +prompt("2. enter note")
//     let avg = (not1 + not2) / 2;
//     console.log(avg);
//     sum = sum + avg;
// }
// let generalAvg = sum / 2;
// console.log(generalAvg);
